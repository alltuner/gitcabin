{
  "name": "gitcabin-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "CLEAN=1 bun run build.ts",
    "watch": "bun run --watch build.ts",
    "clean": "rm -rf ../src/gitcabin/web/static/dist"
  },
  "//": "Everything here is build-time. The bun bundler consumes htmx + tailwind at build, produces hashed JS/CSS into ../src/gitcabin/web/static/dist/, and the runtime container serves the static files. Nothing under node_modules ships.",
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "htmx-ext-preload": "^2.1.1",
    "htmx.org": "4.0.0-beta2",
    "tailwindcss": "^4.0.0"
  }
}
